// src/app/subjects/subjects-inner.tsx
"use client";

import { useMemo } from "react";
import { useSearchParams, useRouter } from "next/navigation";
// … dein bisheriger SubjectsPage-Code hierher verschoben …
export default function SubjectsInner() {
  // HIER deinen bisherigen Code aus /subjects/page.tsx einsetzen,
  // der useSearchParams/useRouter nutzt.
}